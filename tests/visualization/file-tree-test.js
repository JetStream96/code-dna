const fileTree = require('../../src/visualization/file-tree')
const miniTest = require('../mini-test')
const [test, assertEquals] = [miniTest.test, miniTest.assertEquals]

test(() => {

}, 'mergeChildren test')
